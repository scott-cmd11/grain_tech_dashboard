# =============================================================================
# GrainTech Dashboard - Environment Variables
# =============================================================================

# SERVER-SIDE ONLY (Never prefix with VITE_ - keeps key secure on server)
GEMINI_API_KEY=PLACE_YOUR_API_KEY_HERE

# Cron job authentication (required for /api/cron endpoints)
CRON_SECRET=GENERATE_A_SECURE_RANDOM_STRING

# CLIENT-SIDE (VITE_ prefix exposes to browser - only use for public keys)
# Note: Restrict this key in Google Cloud Console to your domain only
VITE_GOOGLE_MAPS_API_KEY=PLACE_YOUR_MAPS_API_KEY_HERE
